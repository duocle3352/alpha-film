import { AlphaTitle } from '~/components/AlphaTitle';

function Setting() {
    return (
        <section>
            <AlphaTitle title={'Setting'} />
            <p>Chức năng chưa hoàn thiện vui lòng quay lại sau!</p>
        </section>
    );
}

export default Setting;
